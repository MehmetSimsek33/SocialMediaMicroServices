server:
  port: 9090

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: admin
    password: 6352
    url: "jdbc:postgresql://localhost:5432/AuthDb"
  jpa:
    hibernate:
      ddl-auto: update
